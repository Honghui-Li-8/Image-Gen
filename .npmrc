approve-builds[]=bcrypt
approve-builds[]=better-sqlite3

# pnpm v11 auto-runs "pnpm install --production" before each script,
# which strips devDeps (including @types/* and tsc) before tsc needs them.
check-deps-before-run=false

